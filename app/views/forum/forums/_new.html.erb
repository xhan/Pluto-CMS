	<% remote_form_for([:forum,@forum]) do |f| %>
	  <%= f.error_messages %>
		 <%= f.label :name, "Name" %>
		<%= f.text_field :name %>
		<br/>
		<%= f.text_area :description,:cols=>30,:rows=>5 %>
	  <p>
	    <%= f.submit 'Create' %>
	  </p>
	<% end %> 